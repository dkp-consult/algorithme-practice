/**
 * Function to calculate the maximum profit from buying and selling a stock.
 * @param {number[]} prices - An array where prices[i] is the price of a given stock on the ith day.
 * @return {number} - The maximum profit achievable from one transaction. Returns 0 if no profit is possible.
 */

function app(prices) {
    // Implement the logic to find the maximum profit
}

module.exports = app;
